services:
  - type: web
    name: sparrowx-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health